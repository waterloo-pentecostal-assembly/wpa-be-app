import 'package:flutter/material.dart';

class AllBibleSeriesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('all bible series')
    );
  }
}